# 2026-06-22 - Launch Pipeline Completion & Microsoft Store Submission

Today, we concluded implementation of the Clyde & Clyde Go launch pipeline. The user confirmed that the desktop app has already been submitted to the Microsoft Store as a standard MSI/EXE package, which simplifies deployment and mitigates sandboxing constraints.

## Accomplished
- **Microsoft Store Submission**: Acknowledged that the MSI/EXE package has been submitted. This preserves normal local folder/socket access, removing any risks of sidecar isolation or port-mapping virtualization failures.
- **Local BYOK Cryptographic Licensing**: Verified offline licensing using RSA public key verification (`validateLicenseKey`).
- **Stripe Billing webhook**: Implemented webhook payment hooks for the `clyde_byok_lifetime` one-time purchase, generating the signed key and sending it via the Resend welcome email flow.
- **Solopreneur AI Agents**: Created HN/Reddit scan agent and support/error logs triage agent.
- **Clyde Go Extension Pacing & Source Selector**: Added randomized human-like delay and STAR prep guide options.
- **Stripe Price Verification & Configuration**: Created the BYOK Lifetime product and price ID (`price_1Tl09WBmOXAq5RyDnR6hszMb`) in Stripe. Verified and mapped active price IDs for all tiers in `.env.local` and Vercel.
- **Pricing UI & Checkout Integration**: Added the "BYOK Lifetime" tier card to the pricing page, fixed a Credit Pack checkmark bug, and enabled checkout redirects.
- **Node Crypto Import Fix**: Fixed a serverless function activation crash by explicitly importing Node's `crypto` module in `_billing.js`.
- **BYOK Private Key Decryption Fix**: Resolved the `error:1E08010C:DECODER routines::unsupported` serverless crash. Identified that the `BYOK_PRIVATE_KEY` env variable configured in Vercel had a different SHA-256 hash (corrupted/wrong key) compared to the correct key in `byok_keys.json`. Corrected the key in Vercel via CLI and added robust normalization to strip surrounding quotes and sanitize escaped newlines.
- **Transactional Idempotency Optimization**: Refactored checkout activation in `_billing.js` to set the Stripe session metadata `processed: 'true'` at the *end* of each success path rather than the beginning. This ensures any backend failure during processing allows a retry to run the activation again.
- **Git Push**: Staged, committed, and pushed the updates to the GitHub remote repository.
- **Test Validation**: Executed all unit tests for license validator and entitlements gating, which passed successfully.

