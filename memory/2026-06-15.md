# June 15, 2026 - Daily Memory

## What Happened
- Investigated a low-contrast and legibility issue in the Calendar Day view under light themes (`theme-snow` and `theme-blossom`), where the entire calendar main pane was rendered as a dark slate container block (`rgba(10, 10, 15, 0.4)` background) with dark-on-dark text and unreadable event description blocks.
- Diagnosed that `.calendar-body` defaulted to a dark-theme glass background without any theme-specific overrides, and `.calendar-day-view` used a transparent background showing the dark panel through.
- Added comprehensive light-theme CSS overrides inside the `calendarStyles` template literal in `src/renderer/App.jsx` for `.theme-snow` (Nordic Snow) and `.theme-blossom` (Sakura Blossom):
  - Overrode `.calendar-body` to render with a light, frosted panel background and appropriate border/shadows.
  - Overrode `.calendar-day-view` background, day header h2 text colors, and empty events text styles.
  - Overrode `.calendar-event-card.large` to render with a light background, and set text elements (`.event-time`, `.event-title`, `.event-entity`, and `.event-desc`) to high-contrast theme-appropriate colors (`var(--text)`, `var(--cyan)`, and custom solid/near-solid light backgrounds for the description block).
  - Overrode standard `.calendar-event-card` and cell backgrounds (`.calendar-day`, `.week-day-col`, `.week-day-header`) for clean month and week views.
  - Replaced hardcoded neon cyan `.view-toggles button.active` styling with premium, theme-colored button toggles using `var(--cyan)` (indigo for snow, rose for blossom) with white text and drop shadows.
  - Polished calendar control buttons (`Today`, `<` `>`) and action buttons to render with clean borders and light backgrounds under light themes.
- Fixed a contrast issue in the floating active source selector popover (`.active-source-menu`) under light themes, where label texts and the "Confirm sources" button text inherited light-theme dark colors (`#1e293b` or `#4c0519`), making them unreadable against the hardcoded dark background (`rgba(10, 13, 20, 0.96)`). Added CSS overrides in `src/renderer/App.css` to force these texts to stay light (`#f1f5f9` and `#ffffff`) with an enhanced hover state.
- Fixed low-contrast text color on the pre-call prep Regenerate button in light themes (`theme-snow` and `theme-blossom`) by adding CSS overrides in `src/renderer/App.css` for `.context-panel button.ghost.compact`. This forces a high-contrast theme-styled border, light background, and dark text color that overrides dynamic JavaScript-driven `--panel` variable overwrites.
- Updated the AI Match Score badge inside the AI Match Analysis panel (`src/renderer/App.jsx`) to match user mock annotations: removed the star icon (`★`), prepended the text `"Match Score "`, and appended `"/5"` to the score output (e.g. `"Match Score 4.6/5"`).
- Fixed a contrast issue in the AI Match Analysis panel (within the timeline/Opportunity Tracker view) under light themes, where the description spans for Top Strength, Main Gap, and Mitigation used the variable `var(--text)` (which is dark slate/maroon in light themes), making them illegible against the hardcoded dark slate container background (`rgba(15, 23, 42, 0.45)`). Modified `src/renderer/App.jsx` to use a light color literal (`#f1f5f9`) for these descriptions.
- Updated `test/rendererSmoke.test.js` to assert `'startCheckout'` instead of the outdated `'startCheckoutSession'`, restoring a fully passing smoke test suite.
- Polished the Settings UI by making the description text under "Account and billing" conditional on the `signedIn` state. It now displays "Your desktop app is connected to your Clyde account." when signed in, preventing confusion for users who are already logged in.
- Moved the Setup Wizard launch button from the Settings drawer header to the General tab within SetupFields. Passed the `onOpenOnboarding` prop and structured the button inside a card layout next to the Service Check action card.
- Built and bundled all renderer assets successfully using `npx vite build`.
- Validated that the entitlements unit tests (`node test/entitlements.test.js`) and the renderer smoke tests (`node test/rendererSmoke.test.js`) pass completely.


